# Dependencies (do NOT ignore package.json / package-lock.json — they are needed to install)
/node_modules
Backend/node_modules
Frontend/node_modules

# Build output
Frontend/dist
dist

# Logs / OS
*.log
.DS_Store

# Editor
.idea
.vscode
